{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<div class="container">
    <h1>Edit Product</h1>

    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="mt-2">{{ include('product/_delete_form.html.twig') }}</div>
    <a class="btn btn-warning mt-2" href="{{ path('app_product_index') }}">Back To Products</a>
</div>
{% endblock %}
